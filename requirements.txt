# ── 基础运行时依赖 ────────────────────────────────────────
fastapi==0.136.0          # Web 框架，提供 /input 与 /parse 接口
uvicorn==0.30.6           # ASGI 服务器，用于本地运行 FastAPI
pydantic==2.13.3          # 数据模型校验（ParsedFile 等）
httpx==0.28.1             # FastAPI 测试客户端的底层依赖
PyYAML==6.0.3
requests==2.32.3          # LLM HTTP 调用

tree-sitter==0.23.2       # Tree-sitter Python 绑定
tree-sitter-python        # Python grammar
tree-sitter-javascript    # JavaScript grammar
tree-sitter-typescript    # TypeScript / TSX grammar
tree-sitter-java          # Java grammar
tree-sitter-go            # Go grammar
tree-sitter-rust          # Rust grammar
tree-sitter-c             # C grammar
tree-sitter-cpp           # C++ grammar

# ── 开发/测试依赖 ────────────────────────────────────────
pytest==9.0.3             # 单元/集成测试框架
pytest-cov==5.0.0         # 可选：代码覆盖率统计（CI 中使用）

# ── 可选工具（根据需要自行启用）──────────────────────────────
langchain==0.2.0

# ── 虚拟环境建议 ────────────────────────────────────────
# 建议使用 Python 3.13+（项目已在 3.13.5 上测试）
